"use strict";
// Difine the list of users
let users = [];
// List of users is not empty
if (users.length === 0) {
    console.log("we need to find some users!");
}
else {
    // list is not empty,remove all usersnames
    users = [];
    console.log("all usernames haue been removed");
}
